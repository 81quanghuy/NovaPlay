spring:
  config:
    import: optional:configserver:http://localhost:8888/
    activate:
      on-profile: dev

auth:
  jwt:
    issuer: novaplay-auth
    audience: novaplay
    kid: v1
    expiration: 900000
    refresh-token:
      expiration: 2592000000
