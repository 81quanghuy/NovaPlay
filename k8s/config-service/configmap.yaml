apiVersion: v1
kind: ConfigMap
metadata:
  name: config-service-config
  labels:
    app: config-service
# Chỉ chứa giá trị không nhạy cảm. Credential nằm ở config-service-secrets.
data:
  SERVER_PORT: "8500"
  TRACING_SAMPLE_RATE: "0.1"
