name: user-service CI

on:
  push:
    branches: [main]
    paths:
      - 'user-service/**'
      - 'utils/**'
      - 'pom.xml'
      - '.github/workflows/user-service-ci.yml'
  pull_request:
    paths:
      - 'user-service/**'
      - 'utils/**'
      - 'pom.xml'
      - '.github/workflows/user-service-ci.yml'

# Push mới lên cùng một branch sẽ huỷ lượt chạy đang dở.
concurrency:
  group: user-service-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: maven

      # user-service phụ thuộc vn.iotstar:utils, mà module này phải nằm trong local repo
      # trước khi build được. -N cài pom cha, vốn không được `install -pl utils` cài giúp.
      - name: Install parent POM and utils
        run: |
          ./mvnw -B -q install -N
          ./mvnw -B -q install -pl utils -DskipTests

      - name: Build and test user-service
        run: ./mvnw -B verify -pl user-service -am

      - name: Upload test reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: surefire-reports
          path: user-service/target/surefire-reports/
          retention-days: 7

  docker-build:
    runs-on: ubuntu-latest
    needs: build-and-test

    steps:
      - uses: actions/checkout@v4

      # Chỉ kiểm chứng image build được. Chưa push vì chưa cấu hình registry.
      - name: Build Docker image
        run: docker build -f user-service/Dockerfile -t novaplay/userservice:${{ github.sha }} .
